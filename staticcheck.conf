# Protocol and provider names intentionally lead some client-facing errors
# (for example, "Anthropic ..." and "Chat ..."). Keep all correctness,
# simplification, and unused-code checks enabled while exempting only that
# stylistic capitalization rule.
checks = ["inherit", "-ST1005"]
